Webserver setup instructions

have python3

add git branch Sean

cd Server

source lvenv/bin/activate (linux) or venv\bin\activate.ps1 (windows)

python3 manage.py runserver

server should start running
